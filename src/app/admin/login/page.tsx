"use client";
import {useState} from "react";import {signIn} from "next-auth/react";import {useRouter} from "next/navigation";import Link from "next/link";import {ArrowRight,LockKeyhole} from "lucide-react";
export default function Login(){
 const router=useRouter();const [error,setError]=useState("");const [loading,setLoading]=useState(false);
 async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);const f=new FormData(e.currentTarget);const r=await signIn("credentials",{email:f.get("email"),password:f.get("password"),redirect:false});if(r?.error){setError("Email or password is incorrect.");setLoading(false)}else router.push("/admin")}
 return <main className="grid min-h-screen md:grid-cols-2"><section className="relative hidden overflow-hidden md:block"><img src="https://images.unsplash.com/photo-1552566626-52f8b828add9?auto=format&fit=crop&w=1500&q=85" alt="Restaurant tables" className="absolute inset-0 h-full w-full object-cover"/><div className="absolute inset-0 bg-[#17261c]/60"/><div className="absolute bottom-12 left-12 max-w-md text-white"><p className="font-serif text-5xl leading-tight">Your menu.<br/>Always fresh.</p><p className="mt-4 text-white/70">Update dishes, prices and availability in a few simple clicks.</p></div></section><section className="grid place-items-center bg-[#f6f5f0] p-6"><form onSubmit={submit} className="w-full max-w-md rounded-[1.75rem] bg-white p-8 shadow-[0_18px_70px_rgba(28,45,34,.09)]"><div className="grid size-12 place-items-center rounded-full bg-[#e6b867] text-[#1d3023]"><LockKeyhole/></div><p className="eyebrow mt-7">Savor menu studio</p><h1 className="mt-2 font-serif text-4xl">Welcome back</h1><p className="mt-2 text-sm text-[#7b837d]">Sign in to manage your digital menu.</p><label className="mt-7 block"><span className="field-label">Email address</span><input name="email" type="email" required className="input" placeholder="owner@restaurant.com"/></label><label className="mt-4 block"><span className="field-label">Password</span><input name="password" type="password" minLength={8} required className="input" placeholder="Password"/></label>{error&&<p className="mt-4 rounded-lg bg-red-50 p-3 text-sm text-red-700">{error}</p>}<button disabled={loading} className="mt-6 flex w-full items-center justify-center gap-2 rounded-xl bg-[#1e3024] py-3.5 text-sm font-bold text-white">{loading?"Signing in...":"Sign in"}<ArrowRight size={17}/></button><Link href="/" className="mt-5 block text-center text-sm text-[#6f7971]">Back to menu</Link></form></section></main>
}
